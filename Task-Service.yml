spring:
  # Database setup
  datasource:
    url: jdbc:mysql://localhost:3306/taskservice?user=root
    username: root
    password: 7999389177
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  graphql:
    websocket:
      path: /graphql
      enabled: true
